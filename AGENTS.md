## Agent skills

### Issue tracker

Issues live in GitHub Issues (`dikology/umbrella-web`), managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.

### Identity and auth authority

Identity language and the auth contract are defined in `umbrella-api`: see `../umbrella-api/CONTEXT.md` and `../umbrella-api/docs/adr/`.
