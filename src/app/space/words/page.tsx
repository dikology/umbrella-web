import MarkedWords from '@/components/MarkedWords';

export const metadata = {
  title: 'Marked Words - Umbrella',
  robots: 'noindex',
};

export default function MarkedWordsPage() {
  return <MarkedWords />;
}
